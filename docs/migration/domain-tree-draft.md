{
  "summary": "Inventory every capability surface that must land in the single grouped `ds` CLI, and classify it into domains",
  "agentCount": 10,
  "logs": [
    "inventoried 301 entries across 5/5 sources",
    "3 independent domain-tree proposals"
  ],
  "result": {
    "final": "## Domain tree\n\nNine domains, printed in this order. Deepest path is three tokens past `ds`. Two-token group nodes exist only where a group has two or more children (C's singleton rule), so `network apply`, `pls section-orientation`, `map tiles`, `report bundle` and `report collisions` are flat.\n\n```\nds project   — bind the work\n  project list                    List your projects for one lifecycle bucket.\n  project open                    Switch the paired desktop to one verified project.\n  project admin-units             List Rwanda admin unit names and codes for one level.\n  project task read               Read one Project Work task hierarchy and its context.\n  project task commit             Apply one revisioned Project Work command.\n  project feedback submit         File an obstacle into the shared feedback backlog.\n  project feedback list           Read the obstacle backlog, mine or everyone's.\n\nds survey    — field data and the forms that collect it\n  survey query                    Typed ad-hoc survey row query, one bounded job per call.\n  survey dashboard                Read one bounded view of the cached survey dashboard.\n  survey forms list               List this project's survey forms and their enabled state.\n  survey forms settings           Read one form's effective project settings and revision.\n  survey forms diagnose           Report incoherent settings and unsatisfiable rules.\n  survey forms commit             Enable/disable forms and write project form settings.\n  survey schema read              Read one form's GLOBAL schema and version.\n  survey schema field-types       List field types, condition operators and aliases.\n  survey schema commit            Create or update a GLOBAL form schema, all projects.\n\nds map       — the open map, GIS analysis, tiles and QGIS\n  map connect                     Grant this session a folder or an exact set of files.\n  map import                      Import granted files as Temporary map layers.\n  map layer add                   Create a Temporary sketch layer from GeoJSON features.\n  map layer remove                Remove a Temporary layer this session created.\n  map zoom                        Fit the open map to a bbox with bounded padding.\n  map outliers                    Find geometry outliers in a layer; writes a sketch layer.\n  map points-along                Generate points at an interval along a layer's geometry.\n  map random-points               Generate spaced random points inside an area layer.\n  map qgis discover               Probe bounded locations for QGIS, PyQGIS and the plugin.\n  map qgis status                 Verify the confined DS-profile integration generation.\n  map qgis plan                   Mint a short-lived plan for a confined QGIS change.\n  map qgis apply                  Apply a signed plan: stage the plugin, swap the profile.\n  map tiles                       Build design and survey vector tiles as a durable job.\n\nds design    — LV/MV design rooms\n  design status                   Read bounded LV/MV design status metadata.\n  design gaps                     List surveyed areas not yet designed for one project.\n  design transformer read         Layer names, feature counts and optional histogram.\n  design transformer save         Push the dirty local room to the project.\n  design features select          Resolve a selection by layer, where, bbox or ids.\n  design features create          Stage GeoJSON as draft design rows in the local room.\n  design features set             Stage a bulk property write into the local room.\n  design process                  Run Fast LV on one transformer; stages locally.\n\nds network   — canonical .dsgrid models\n  network inspect                 Identify a .dsgrid model and inventory what is in it.\n  network validate                Verify a package and run full model validation.\n  network summary                 Deterministic content summary of one .dsgrid package.\n  network describe                Engine command, operation and projection descriptors.\n  network convert inspect         Classify each source and print the capability matrix.\n  network convert plan            Digest-pinned conversion plan with blockers and losses.\n  network convert run             Execute the plan; write artifacts and exchange-report.\n  network compose inspect         Per-source identity, digest, CRS and conflict inventory.\n  network compose plan            Digest-pinned composition plan with source lineage.\n  network compose run             Write the composed .dsgrid and composition-report.json.\n  network check section           Run the span-envelope structure check on one section.\n  network check usage             Model-wide CRI stringing plus usage screening.\n  network check deflections       Interior route-node deflections under a tolerance.\n  network stringing               Stringing intent and solved per-case conductor states.\n  network pack                    Pack snapshot.json plus assets into a verified .dsgrid.\n  network unpack                  Verify, then materialize package members to a directory.\n  network evidence                Write package, component JSON and offline profile atlas.\n  network apply                   Apply one command envelope; write the next revision.\n  network library pack            Pack a snapshot into an immutable library release.\n  network library unpack          Read a release manifest, summary and asset leaves.\n  network library verify          Verify a release against an expected sha256 digest.\n  network library store           Store a release into a content-addressed store dir.\n  network library open            Open a stored release by artifact id and revision id.\n  network library catalog         Derive the catalog entry for one release file.\n  network template compile        Compile .dsgrid/.bak/.cri/.str into a .dsgrid-template.\n  network template inspect        Read a template manifest, summary and assets.\n  network template export-pls     Export a native PLS library artifact from the model.\n  network seed                    Freeze sources into a content-addressed cloud seed tree.\n\nds pls       — PLS-CADD workspaces, structures, Oracle\n  pls versions                    Dump the PLS-CADD version-contract catalog.\n  pls structure ingest            Ingest a Method 1/2 structure; packs only with --out.\n  pls structure emit              Re-emit a native analytical structure from the model.\n  pls structure roundtrip         Byte and fingerprint roundtrip verdict for a structure.\n  pls structure audit             Classify each embedded .012 as Method 1/2/mixed.\n  pls structure usage             Parse the Structure Locations and Usage RTF table.\n  pls structure available         Project the exact Available Structure List columns.\n  pls pole-capacity read          Read one pole capacity block, digest-pinned and paged.\n  pls pole-capacity assign        Write a new structure resource by verified capacity edit.\n  pls section-orientation         Diagnose a DON section against a declared alignment.\n  pls reference-closure           Translate a live workspace's reference closure.\n  pls compare-don                 Reconcile a DON's structure assignment vs an authority.\n  pls postprocess                 Graft native template categories into a new workspace.\n  pls oracle candidate            Write quarantined Stage 2/3/4 qualification artifacts.\n  pls oracle submit               Validate and queue a closed PLS-CADD Oracle job.\n  pls oracle status               Report a job's queue, claim and success state.\n  pls oracle result               Verify and return a job result, digest and report paths.\n\nds solar     — solar batches\n  solar verify-weather            Re-derive a weather dataset's digest and cache key.\n  solar prepare                   Resolve weather cache-first; commit prepared city inputs.\n  solar run start                 Run prepared cities; writes results, charts, batch.json.\n  solar run progress              Return the newest whole step table for a run.\n  solar run result                Return a run's terminal status, errors and artifacts.\n  solar run cancel                Stop a run this machine started and seal its receipt.\n  solar result read               Read a bounded projection of one city's result document.\n  solar report                    Resolve the APD the calculation already wrote.\n  solar workspace list            List solar city contexts, readiness and batch runs.\n  solar workspace choose          Approve this project's solar working directory.\n  solar document compose          Write authored Markdown into the run's quarantined dir.\n  solar document list             List a batch's documents with digests and publish state.\n  solar document read             Read a workspace Markdown document in bounded slices.\n  solar compare                   Compare two result documents by canonical digest.\n  solar request                   Emit the cloud calculate request body for one input.\n  solar parity                    Compare the engine against the frozen golden summary.\n  solar calculate                 Cloud-lane calculation and report as a governed job.\n  solar publish                   Publish a local run's artifacts to the governed record.\n\nds report    — deliverables\n  report engine                   Reporter identity and its published task schemas.\n  report admin-bounds             Build the deterministic .dsab admin-bounds index.\n  report transformer build        Build one transformer's SHP/KMZ/XLSX from local bytes.\n  report transformer export       Export one transformer from the paired desktop's rooms.\n  report transformer errors       Transformer-error report with worker attribution.\n  report combined build           Build combined SHP/XLSX/GeoJSONSeq from local bytes.\n  report combined export          Export one combined version over the desktop's rooms.\n  report bundle                   Seal per-transformer and combined reports into one ZIP.\n  report collisions               Run the project's transformer collision detection.\n  report print                    Compose GeoJSON and PMTiles onto paper as PDF/PNG.\n\nds desktop   — the paired DS GridDesign session\n  desktop status                  Report pairing, sign-in and selected project.\n  desktop state                   Read the paired desktop's map and app snapshot.\n```\n\n112 commands. `ds capabilities`, `ds doctor`, `ds version`, `ds help` stay root meta commands outside the domain list, exactly as `crates/ds/src/registry.rs` already has them.\n\nTwo disambiguation rules are load-bearing and must be enforced in help text, because `ds capabilities --search \"inspect\"` returns these adjacent: `ds network inspect` takes `--model` and its first help line must say **model identity** (\"which .dsgrid is this, what is in it\"); `ds network convert inspect` takes `--source` and its first line must say **source classification** (\"what format are these files, what can they become\"). Same for `network template inspect` (manifest identity) and `network compose inspect` (per-source conflict inventory).\n\n## Root help preview\n\n`ds --help` prints exactly this and nothing else — no command names, no availability probes:\n\n```\nds — the Data Solutions command line.\n\nUSAGE\n  ds <domain> <command> [--flags]\n\nDOMAINS\n  project  Bind the work: projects, tasks, feedback, admin units.\n  survey   Field data and the forms that collect it.\n  map      The open map: layers, GIS analysis, tiles and QGIS.\n  design   LV/MV design rooms: features, Fast LV process, save.\n  network  Canonical .dsgrid models: inspect, convert, compose, verify.\n  pls      PLS-CADD workspaces, structures, and the Oracle loop.\n  solar    Solar batches: prepare, run, results, documents.\n  report   Deliverables: transformer, combined, bundle, collisions.\n  desktop  The paired DS GridDesign session: pairing, sign-in, project.\n\nDISCOVERY\n  ds <domain> --help             commands in one domain\n  ds <domain> <cmd> --help       one command's full contract\n  ds capabilities [<domain>|<id>]  the same, as JSON\n  ds capabilities --search <text>  find a command by words\n  ds doctor                      what works here, and why not\n\nGLOBAL\n  --output human|json  stdout format (default: human)\n  --pretty             indent JSON; costs bytes, aids humans\n  --no-color           never emit ANSI (also honours NO_COLOR)\n  --yes                pre-confirm an effectful command\n  --version            build identity\n\nMachine output is stdout; diagnostics are stderr.\nExit  0 ok · 2 input · 3 unavailable · 4 auth · 5 conflict · 6 failed · 1 bug\n```\n\n**1390 bytes** (measured by rendering through `help::root` in `crates/ds-cli-contract/src/help.rs:35` with the nine domain rows above). Today's two-domain root help is 949 bytes; the ceiling in `crates/ds/tests/context_budget.rs::root_help_is_cheap` is 1_400, so **the nine-domain root fits with 10 bytes of headroom and needs no budget change** — but adding a tenth domain would breach it, which is exactly the signal that test exists to give.\n\n**Tier-2 resolution (graft 9).** `help::domain` at `crates/ds-cli-contract/src/help.rs:78` prints one row per leaf with the full sub-path joined. A flat 28-leaf `ds network --help` measures **2339 bytes** against the 900-byte ceiling in `domain_help_is_cheap`. Fix it in two moves, in one commit: (a) collapse any multi-child group to a single index row carrying its children in parentheses — `convert  Classify, plan and execute a format conversion.  (inspect, plan, run)` — which takes `ds network --help` to 15 rows and **1284 bytes**, `ds pls --help` to 8 rows, `ds solar --help` to 12, `ds map --help` to 10; and (b) raise the tier-2 ceiling from 900 to 1_400 in `context_budget.rs` with the reason written into the test, per CLAUDE.md's rule against silent relaxation. Never one without the other.\n\n**Authority as a query, not a tree (graft 10).** `Effect` and `Authority` are already declared fields on every `Command` in `crates/ds-cli-contract/src/spec.rs`, and `ds capabilities` already supports `--search`. Add `--authority <none|desktop_pairing|desktop_user|project>` and `--effect <discovery|read_only|local_ui|local_file_write|artifact_write|global_write>` filters to `ds capabilities`. Those flags are documented at tier 3 (`ds capabilities --help`), not at root, so B's entire organising insight — \"show me everything that runs offline\", \"show me everything that mutates shared state\" — costs zero root-help bytes and zero tree churn.\n\n## Migration matrix\n\n| ds command | replaces (verbatim) | owner (computes) | effect | authority | disposition | priority |\n|---|---|---|---|---|---|---|\n| ds project list | `desktop_list_projects`, `project.list` | ds-brain API via ds-web desktop bridge | read_only | desktop_user | merge | later |\n| ds project open | `desktop_open_project`, `project.open` | ds-web desktop bridge (project.open) + ds-brain API | local_ui | desktop_user | merge | later |\n| ds project admin-units | `admin.units` | ds-brain API | read_only | desktop_user | migrate | later |\n| ds project task read | `read_project_work_task` | ds-brain (ProjectWorkTaskRead) | read_only | project | migrate | later |\n| ds project task commit | `commit_project_work_command` | ds-brain (canonical command boundary) | global_write | project | migrate | later |\n| ds project feedback submit | `report_obstacle`, `feedback.submit` | ds-brain feedback ledger (brain or desktop route) | global_write | project | merge | later |\n| ds project feedback list | `read_feedback`, `feedback.mine`, `feedback.list` | ds-brain feedback ledger | read_only | project | merge | later |\n| ds survey query | `query_survey_data` | ds-brain (one bounded BigQuery job) | read_only | project | migrate | later |\n| ds survey dashboard | `query_survey_dashboard` | ds-brain (cached reports projection) | read_only | project | migrate | later |\n| ds survey forms list | `read_project_forms` | ds-brain (ProjectFormsRead) | read_only | project | migrate | later |\n| ds survey forms settings | `read_project_form_settings` | ds-brain (ProjectFormSettingsEditor) | read_only | project | migrate | later |\n| ds survey forms diagnose | `diagnose_project_forms` | ds-brain (ProjectFormsDiagnose) | read_only | project | migrate | later |\n| ds survey forms commit | `commit_project_form_settings` | ds-brain (bulk_save contract) | global_write | project | migrate | later |\n| ds survey schema read | `read_form_definition` | ds-brain (FormDefinitionRead) | read_only | project | migrate | later |\n| ds survey schema field-types | `describe_form_field_types` | ds-brain (FormFieldTypes) | read_only | project | migrate | later |\n| ds survey schema commit | `commit_form_definition` | ds-brain (CommitFormDefinition) | global_write | project | migrate | last |\n| ds map connect | `folder.connect`, `files.connect` | ds-web frontend (native picker) | local_ui | desktop_pairing | merge | later |\n| ds map import | `map.add_layers_from_folder` | ds-web frontend (human import pipeline) | local_ui | desktop_pairing | migrate | later |\n| ds map layer add | `map.temporary_layer.add` | ds-web frontend | local_ui | desktop_pairing | migrate | later |\n| ds map layer remove | `map.temporary_layer.remove` | ds-web frontend | local_ui | desktop_pairing | migrate | later |\n| ds map zoom | `map.zoom_to` | ds-web frontend | local_ui | desktop_pairing | migrate | later |\n| ds map outliers | `gis.detect_geometry_outliers` | ds-web frontend (shared analysis modules) | local_ui | desktop_pairing | migrate | later |\n| ds map points-along | `gis.points_along` | ds-web frontend (shared analysis modules) | local_ui | desktop_pairing | migrate | later |\n| ds map random-points | `gis.random_points` | ds-web frontend (shared analysis modules) | local_ui | desktop_pairing | migrate | later |\n| ds map qgis discover | `qgis_discover_installations` | ds-cli (port of ds-mcp internal/qgisdiscover) | read_only | none | migrate | near |\n| ds map qgis status | `qgis_integration_status` | ds-cli (port of ds-mcp internal/qgismanage) | read_only | none | migrate | near |\n| ds map qgis plan | `qgis_integration_management_plan` | ds-cli (port of internal/qgismanage) | read_only | none | defer | last |\n| ds map qgis apply | `qgis_integration_apply_change` | ds-cli + pinned qgis_mcp_plugin archive | local_file_write | none | defer | last |\n| ds map tiles | `build_vector_tiles` | ds-vector-tiler via ds-brain | artifact_write | project | defer | last |\n| ds design status | `query_network_design` | ds-brain (DesignProjection) | read_only | project | migrate | later |\n| ds design gaps | `query_surveyed_not_designed` | ds-brain business/data API | read_only | project | defer | last |\n| ds design transformer read | `design_read_transformer`, `design.transformer.read` | ds-web desktop bridge + ds-brain fallback | read_only | desktop_user | merge | later |\n| ds design transformer save | `design_save_transformer`, `design.transformer.save` | ds-brain API via ds-web desktop bridge | global_write | desktop_user | merge | later |\n| ds design features select | `design_select_features`, `design.features.select` | ds-web desktop bridge | read_only | desktop_user | merge | later |\n| ds design features create | `design_create_features`, `design.features.create` | ds-web desktop bridge | local_ui | desktop_user | merge | later |\n| ds design features set | `design_set_feature_properties`, `design.features.set_properties` | ds-web desktop bridge | local_ui | desktop_user | merge | later |\n| ds design process | `design_process_transformer`, `design.process.run` | ds-network Fast LV kernel via the bridge | local_ui | desktop_user | merge | later |\n| ds network inspect | `inspect` | ds-grid-exchange + ds-grid-model | discovery | none | migrate | first-slice |\n| ds network validate | `validate` | ds-grid-model (unpack via ds-grid-exchange) | read_only | none | migrate | near |\n| ds network summary | `summary` | ds-grid-model | read_only | none | migrate | near |\n| ds network describe | `describe commands`, `describe operations`, `describe projections` | ds-grid-engine | discovery | none | merge | near |\n| ds network convert inspect | `convert-inspect` | ds-grid-exchange | read_only | none | migrate | near |\n| ds network convert plan | `convert-plan` | ds-grid-exchange | read_only | none | migrate | near |\n| ds network convert run | `convert` | ds-grid-exchange | local_file_write | none | migrate | near |\n| ds network compose inspect | `compose-inspect` | ds-grid-exchange | read_only | none | migrate | near |\n| ds network compose plan | `compose-plan` | ds-grid-exchange | read_only | none | migrate | near |\n| ds network compose run | `compose` | ds-grid-exchange | local_file_write | none | migrate | near |\n| ds network check section | `check-section` | ds-grid-engine | read_only | none | migrate | near |\n| ds network check usage | `check-structure-usage` | ds-grid-engine | read_only | none | migrate | near |\n| ds network check deflections | `analyze-alignment-deflections` | ds-grid-engine | read_only | none | migrate | near |\n| ds network stringing | `report-section-stringing` | ds-grid-engine | read_only | none | migrate | near |\n| ds network pack | `pack` | ds-grid-exchange | local_file_write | none | migrate | near |\n| ds network unpack | `unpack` | ds-grid-exchange | local_file_write | none | migrate | near |\n| ds network evidence | `evidence` | ds-grid-engine (+ ds-grid-exchange unpack) | local_file_write | none | migrate | near |\n| ds network apply | `session apply` | ds-grid-engine (+ ds-grid-exchange emit) | local_file_write | none | migrate | later |\n| ds network library pack | `library pack` | ds-grid-exchange | local_file_write | none | migrate | later |\n| ds network library unpack | `library unpack` | ds-grid-exchange | read_only | none | migrate | later |\n| ds network library verify | `library verify` | ds-grid-exchange | read_only | none | migrate | later |\n| ds network library store | `library store` | ds-grid-exchange | local_file_write | none | migrate | later |\n| ds network library open | `library open` | ds-grid-exchange | read_only | none | migrate | later |\n| ds network library catalog | `library catalog` | ds-grid-exchange | read_only | none | migrate | later |\n| ds network template compile | `template compile` | ds-grid-exchange | local_file_write | none | migrate | later |\n| ds network template inspect | `template inspect` | ds-grid-exchange | read_only | none | migrate | later |\n| ds network template export-pls | `template export-pls` | ds-grid-exchange | local_file_write | none | migrate | later |\n| ds network seed | `seed` | ds-grid-exchange (+ ds-grid-model) | local_file_write | none | migrate | later |\n| ds pls versions | `describe pls-cadd-versions` | ds-grid-exchange | discovery | none | migrate | near |\n| ds pls structure ingest | `ingest-structure` | ds-grid-exchange | local_file_write | none | migrate | near |\n| ds pls structure emit | `emit-structure` | ds-grid-exchange | local_file_write | none | migrate | near |\n| ds pls structure roundtrip | `roundtrip-structure` | ds-grid-exchange | read_only | none | migrate | near |\n| ds pls structure audit | `audit-structure-methods` | ds-io | read_only | none | migrate | near |\n| ds pls structure usage | `parse-pls-structure-usage` | ds-io | read_only | none | migrate | near |\n| ds pls structure available | `report-available-structures` | ds-grid-exchange | read_only | none | migrate | near |\n| ds pls pole-capacity read | `describe-pole-capacity`, `describe_pole_capacity` | ds-grid-tasks (linked) | read_only | none | merge | near |\n| ds pls pole-capacity assign | `assign-pole-capacity`, `assign_pole_capacity` | ds-grid-tasks (linked) | local_file_write | none | merge | near |\n| ds pls section-orientation | `diagnose-pls-section-orientation`, `diagnose_pls_section_orientation` | ds-grid-tasks over ds-io | read_only | none | merge | near |\n| ds pls reference-closure | `inspect-pls-reference-closure`, `inspect_pls_reference_closure` | ds-grid-tasks over ds-io | read_only | none | merge | near |\n| ds pls compare-don | `compare-don-assignment`, `compare_don_assignment` | ds-grid-tasks (TaskRunner::CompareDonAssignment) | read_only | none | merge | near |\n| ds pls postprocess | `pls-postprocess` | ds-io | local_file_write | none | migrate | later |\n| ds pls oracle candidate | `pls-oracle-candidate` | ds-grid-exchange | local_file_write | none | migrate | later |\n| ds pls oracle submit | `pls_oracle_submit` | ds-pls-cadd-oracle-host (spool) | artifact_write | project | migrate | later |\n| ds pls oracle status | `pls_oracle_status` | ds-pls-cadd-oracle-host (spool) | read_only | project | migrate | later |\n| ds pls oracle result | `pls_oracle_result` | ds-pls-cadd-oracle-host (spool) | read_only | project | migrate | later |\n| ds solar verify-weather | `ds-solar verify-weather` | ds-solar-runtime | read_only | none | migrate | near |\n| ds solar prepare | `ds-solar prepare`, `WeatherPreparer::prepare` | ds-solar-runtime | local_file_write | none | merge | near |\n| ds solar run start | `ds-solar run`, `solar_run_start`, `solar.run.start`, `run_cities`, `run_cities_observed`, `require_prepared` | ds-solar-runtime | local_file_write | none | merge | near |\n| ds solar run progress | `solar_run_progress`, `solar.run.progress` | ds-solar-runtime (batch receipt) | read_only | none | merge | near |\n| ds solar run result | `solar_run_result`, `solar.run.result` | ds-solar-runtime (batch receipt) | read_only | none | merge | near |\n| ds solar run cancel | `solar_run_cancel`, `solar.run.cancel` | ds-solar-runtime (engine process group) | local_file_write | none | merge | later |\n| ds solar result read | `solar_read_result`, `solar.result.read` | ds-solar workspace | read_only | none | merge | later |\n| ds solar report | `solar_report`, `solar.report.generate`, `generate_apd`, `SolarReportModel::build`, `city_charts` | ds-solar-report (resolution only) | read_only | none | merge | near |\n| ds solar workspace list | `solar_workspace`, `solar.workspace.read` | ds-solar workspace | read_only | none | merge | near |\n| ds solar workspace choose | `solar_workspace_choose`, `solar.workspace.choose` | ds-web desktop bridge (native picker + grant) | local_ui | desktop_user | merge | later |\n| ds solar document compose | `solar_compose_document`, `solar.document.compose` | ds-solar workspace (authored directory) | local_file_write | none | replace | later |\n| ds solar document list | `solar_list_documents`, `solar.document.list` | ds-solar workspace | read_only | none | merge | later |\n| ds solar document read | `solar_read_document`, `solar.document.read` | ds-solar workspace | read_only | none | merge | later |\n| ds solar compare | `ds-solar compare` | ds-solar-contracts | read_only | none | migrate | near |\n| ds solar request | `ds-solar request` | ds-solar-contracts | local_file_write | none | migrate | last |\n| ds solar parity | `ds-solar parity`, `generate_full_apd_draft` | ds-solar-runtime + ds-solar-report | read_only | none | migrate | last |\n| ds solar calculate | `solar_calculate`, `solar_report_generate` | ds-solar via a ds-brain durable job | artifact_write | project | defer | last |\n| ds solar publish | `solar_publish`, `SolarArtifactBatch::sync_intent` | ds-brain (single writer) + desktop publish door | global_write | project | defer | last |\n| ds report engine | `ds-report build-info`, `ds-report task-schemas`, `report_engine_identity`, `task_schemas` | ds-network-reporter (ds-report sidecar) | discovery | none | merge | near |\n| ds report admin-bounds | `ds-report prepare-admin-bounds`, `AdminBoundsIndex::prepare_native_asset` | ds-network-reporter (ds-report sidecar) | local_file_write | none | merge | later |\n| ds report transformer build | `ds-report export-transformer-report`, `export_transformer_report` | ds-network-reporter (ds-report sidecar) | local_file_write | none | merge | near |\n| ds report transformer export | `network_report_export_transformer`, `network.report.export_transformer` | ds-network-reporter via ds-web desktop bridge | local_file_write | desktop_user | merge | later |\n| ds report transformer errors | `network_report_transformer_errors` | ds-network-reporter via a ds-brain durable job | artifact_write | project | defer | last |\n| ds report combined build | `ds-report export-combined-transformer-report`, `export_combined_transformer_report`, `export_combined_transformer_report_with_cancellation` | ds-network-reporter (ds-report sidecar) | local_file_write | none | merge | near |\n| ds report combined export | `network_report_export_combined`, `network.report.export_combined` | ds-network-reporter via ds-web desktop bridge | local_file_write | desktop_user | merge | later |\n| ds report bundle | `network_report_export_compounded`, `network.report.export_compounded` | ds-network-reporter via ds-web desktop bridge | local_file_write | desktop_user | merge | later |\n| ds report collisions | `network_report_detect_collisions`, `network.report.detect_collisions` | ds-brain collision action → ds-network-reporter | artifact_write | project | merge | later |\n| ds report print | `canvas_print::render_print`, `print-proof` | canvas-print | local_file_write | none | defer | later |\n| ds desktop status | *(new; replaces the `mcp --stdio` initialize handshake and `DS_MCP_LOCAL_PRINCIPAL`/`DS_MCP_LOCAL_PROJECT` probing)* | ds-cli-desktop (bridge descriptor discovery) | discovery | none | replace | first-slice |\n| ds desktop state | `desktop_get_app_state` | ds-web desktop bridge | read_only | desktop_pairing | migrate | near |\n\nCorrections carried by this matrix, each with a stated reason:\n\n- **QGIS authority drops to `none`** (graft 3). The four `qgis_*` tools probe the local filesystem and a confined DS profile with no session involved; their `desktop_pairing` is an artifact of ds-mcp reaching them over stdio. `ds map qgis discover` must answer on a machine with nothing installed.\n- **Solar authority is per-command, not per-environment** (graft 4). All twelve solar tools declare `AuthLocal` and are mutated to `AuthDesktopUser` when `DS_SOLAR_PLACEMENT=paired`. Only `solar workspace choose` genuinely needs the desktop (native picker + grant). `DS_SOLAR_PLACEMENT` stops being an authority input.\n- **The solar cloud lane is separate commands, not a `--lane` flag** (graft 2). `solar run start`/`solar report` stay authority=none local work; `solar calculate` is the project-authority `artifact_write`; `solar publish` stays the governed `global_write`.\n- **The report offline lane survives** (graft 1). `report transformer build` and `report combined build` take local canonical bytes at authority=none and need no app running; `… export` reads the paired app's rooms at desktop_user. `report collisions` is recorded as `artifact_write` at `project` authority because it POSTs ds-brain `/report` → `POST /v1/detect_collisions`, which writes the project's collisions doc server-side; its declared `local_file_write` is wrong.\n- **`design transformer save`** is `global_write`, which makes `registry::dispatch` require `--yes` in the one place a handler cannot forget. Today the bridge pushes it with no prompt despite `CAPABILITIES` marking `confirmation:true`.\n- **The five typed file tasks become read-only.** `--result FILE` with `create_new` existed to hand a result across a process boundary; in `ds` the result is stdout under the output envelope and `--result` is an optional convenience. Only `pls pole-capacity assign` keeps `local_file_write`.\n- **`compare_don_assignment` is wired for the first time.** It is registered in ds-mcp but `DonAssignmentTasks` is never assigned in either cmd binary (`cmd/ds-mcp/main.go:257`), so it always answers `not_configured`; the live implementation is ds-grid's `compare-don-assignment`.\n- **`solar document compose` is disposition `replace`**, not `merge`: its current description instructs the model to \"Render it with solar_render_document\", a tool that exists nowhere in any repo. That sentence must not be carried forward.\n\n## Dropped\n\n| identifier | reason |\n|---|---|\n| `describe_data_solutions_tools` | ds-mcp's projection of its own registry. `ds capabilities` is generated from the same command table dispatch uses, so there is nothing left to project. |\n| `describe_ds_network_engine` | Static description plus a binary-presence probe for a separate ds-grid-mcp process. In one binary the engine is linked; identity is `ds version` / `ds doctor`. |\n| `solar_capabilities`, `solar.capabilities.read` | Linked-engine identity and ported stages. Build identity: `ds version --output json` and `ds doctor`. |\n| `mcp-schemas` | Renders ds-grid descriptors as MCP tool schemas. Pure transport artifact; `ds capabilities <id> --output json` and `ds network describe` replace both halves. |\n| `desktop_invoke_local_operation` | Generic passthrough over ten closed bridge ops. Each is now a typed command; a command taking an operation name as data would reintroduce the untyped door. |\n| `propose_layer_style` | Model orchestration: an LLM drafts a style and saves nothing. The CLI's caller is already a model. |\n| `apply_layer_style` | Planned, and its only producer is the dropped LLM proposal. Nothing deterministic emits a style to apply, so it gets no command until a real source exists. |\n| `draft_project_task` | Model orchestration: an LLM drafts an editable task. `ds project task read` serves the roster; `ds project task commit` takes the authored command. |\n| `propose_lv_route_candidates` | Vision proposes corridors; the kernel engineers them. The kernel half is `ds design process`; the proposal half is the caller's own work. |\n| `preview_project_report` | An ds-mcp-only confirmation preview that generates nothing. `--yes` plus each command's own plan output is the confirmation contract. |\n| `solar_render_document` | Named in `solar_compose_document`'s description but exists nowhere in the repo. Dead reference; corrected, not ported. |\n| `gis.clip` | Allowlisted and declared in CAPABILITIES but has no case in `executeAgentOperation`; throws not-available-in-this-release. |\n| `style.preview`, `style.save_local` | Declared with no handler; throw not-available-in-this-release. |\n| `style.save_global` | In CAPABILITIES but missing from `LOCAL_OPERATIONS`, so the Rust shell rejects it as `unknown_operation`. |\n| `workspace.save`, `workspace.save_as` | Allowlisted and declared, unimplemented; they throw. |\n| `ds-solar bench` | Wall time, per-city time and peak RSS for a release run. Developer benchmark; stays in ds-solar-cli. |\n| `ds-solar` (binary), `ds-report` (binary), `ds-grid-cli` (binary) | Process boundaries, not capabilities. Their subcommands are placed individually; `ds-report` survives as the invoked sidecar behind `ds report *`, not as an operator surface. |\n| `ds-solar-cloud`, `ds_solar_cloud::router`, `POST /api/v1/solar/calculate` | Cloud Run server, its exported router, and its route. `ds solar request` emits the exact request body; the lane itself is `ds solar calculate` behind ds-brain. |\n| `ds-network-reporter` (service), `ds_network_reporter::router` | Cloud Run service binary and deployed axum router built from GCP env. A CLI must never hold that ambient authority. |\n| `POST /v1/export_transformers`, `POST /v1/export_combined`, `POST /v1/zip_bundle`, `POST /v1/detect_collisions`, `POST /v1/enrich_admin_bounds` | Private reporter endpoints behind ds-brain — the cloud lane of the `ds report` commands, not separate doors. |\n| `POST /v1/add_transfo_to_combined` | Server-side BigQuery mirroring step of the cloud combined lane; an implementation stage, not a user task. |\n| `WeatherPreparer::prepare`*, `SolarRuntime::calculate`, `Pipeline::run`, `run_cities`*, `run_cities_observed`*, `require_prepared`*, `RenderedArtifacts::render`, `SolarArtifactBatch::close`, `SolarArtifactBatch::sync_intent`*, `city_charts`*, `generate_apd`*, `SolarReportModel::build`* | In-process ds-solar library API. (*Listed as sources of the commands that call them; none gets its own door — publishing the engine's seams would publish a second calculation path.) |\n| `generate_full_apd_draft` | Parity-mode APD draft with LLM scaffolding; an oracle-comparison artifact reached through `ds solar parity`, not a product document. |\n| `build_one_per_transformer`, `build_per_transformer`, `build_combined_report`, `build_one_combined_report`, `AdminBoundsIndex::load_from_path`, `canvas_print::renderer::render` | In-memory builders and loaders inside ds-network-reporter and canvas-print: the bodies of the report commands. |\n| `export_local` | Developer proof of the offline export; its own doc comment says it must not be registered as a tool. |\n| `dump_combined_xlsx` | Dev harness reading live Firestore/BigQuery with developer credentials. The CLI must carry no ambient-credential path. |\n| `verify_read_export` | CPU-scaling harness over one live transformer; a benchmark. |\n\n## Deletion targets\n\n**Deletable outright once the tree lands (nothing to prove beyond command parity):**\n\n- **ds-mcp, entirely** — `internal/tools/*.go` (58 registered tools + the 7 `internal/tools/catalog.go` planned entries), `cmd/ds-mcp`, `internal/qgisdiscover` and `internal/qgismanage` (ported to `ds map qgis`), `internal/grid/tasks.go` task-runner shims, `internal/oracle` spool client, `internal/style`, `internal/lv`, `internal/projectwork`, `internal/report_preview`. Go module, `go.mod`, `go.sum`.\n- **Go toolchain rails in ds-web** — `scripts/desktop/prepare-ds-mcp-sidecars.sh` in full (the `go build` at :150, the checkout gate at :52, the `ds-mcp build-info` / `ds-mcp-build/v1` identity contract at :189, the ds-grid-mcp stdio `initialize` handshake at :244, the `ds-mcp-<triple>`/`ds-grid-mcp-<triple>` staging at :283) and its contract test `scripts/desktop/test-prepare-ds-mcp-sidecars.sh:29`; the Go preflight blockers at `desktop-build-linux.sh:91` and `desktop-build-windows.sh:122`; the `DS_MCP_DIR` guard at `desktop-build-windows.sh:66`; `actions/setup-go` keyed on `ds-mcp/go.mod` at `.github/workflows/desktop-windows.yml:97` and the ds-mcp checkout at :62; `go version` in the build-cache identity at `scripts/desktop/build-cache.sh:456`; the `.go`/`go.mod`/`go.sum` FORBIDDEN regex at `scripts/verify-desktop-bundle-policy.sh:77` (the existing Rust rules already cover `.rs`).\n- **The hosted MCP hop** — the run-launcher row `ds-mcp|mcp|ds-mcp|8092` at `scripts/run/services.sh:49` with its TCP readiness probe, `DS_MCP_URL`/`DS_MCP_AUDIENCE` at :194, the nine-key `DS_MCP_UNSET_ENV` scrub at :228, the per-service env at :299 (`DS_MCP_REQUIRE_GATEWAY_IDENTITY`, `DS_NETWORK_ROOT`, `DS_GRID_MCP_BIN`, `DS_SOLAR_*` placement), the `kind=mcp` `go run ./cmd/ds-mcp serve` exec at :388, `ds_grid_mcp_prepare` at :246, the `run-web.sh:223` gate, the `mcp)` branch of `scripts/run/preflight.sh:156`, and the two asserting tests `src/tests/run-launcher.test.ts:326` and `:849`.\n- **The Go VCS-stamp workaround** — `windows-deploy.sh:218` clones ds-mcp instead of using a worktree only because Go 1.26 drops VCS stamps from linked worktrees. Cargo needs no such hack; the clone reverts to a worktree.\n- **ds-network's second binary** — `ds-grid-mcp` (the crate and the `DS_GRID_MCP_BIN` contract), once `network describe`, `network convert *` and the five `pls` file tasks are linked. `crates/ds-grid-cli` itself is deletable only after every one of its 45 subcommands has a `ds` equivalent shipping; until then it stays as the reference implementation the contract tests compare against.\n- **The five dead bridge operations** — `gis.clip`, `style.preview`, `style.save_local`, `workspace.save`, `workspace.save_as` in `src/lib/desktop/agent-bridge.ts` CAPABILITIES, plus the orphan `style.save_global` declaration, plus the `desktop_invoke_local_operation` dispatcher itself.\n\n**Renames, not deletions (must land in one commit each, everywhere):**\n\n- `externalBin` goes from `ds-report` + `ds-mcp` + `ds-grid-mcp` to `ds-report` + `ds`, asserted in three places that must change together: `src-tauri/tauri.desktop-runtime.conf.json:4`, `desktop-build-windows.sh:513`, `scripts/verify-desktop-bundle-policy.sh:56`; plus the installed-package presence check at `verify-desktop-bundle-policy.sh:94` and `.gitignore:68`.\n- The component id `ds-mcp` → `ds`: `desktop-components.json:141`, `src/lib/desktop/components.ts:182`, `src-tauri/src/component_manager.rs:505` and `:755`, the i18n keys `pnl_component_ds_mcp_label`/`_description` at `src/lib/i18n/panels.ts:224`, the fixture tests at `src/lib/desktop/components.test.ts:295`, and the core-set ordering assertion `[ds-web, ds-network, ds-mcp, network-reporter]` at `src/tests/solar-addition-delivery-contract.test.ts:11`.\n- `mcp_source_sha` → a cli source sha: the `git rev-parse ../ds-mcp` probe at `scripts/build-desktop.mjs:176`, the metadata field at `:277`, the drift check at `desktop-release-lib.sh:295`, the field-list test at `scripts/desktop/test-build-cache.sh:221` and `src/tests/desktop-build-env.test.ts:144`. Also `DS_RELEASE_PIN_DS_MCP` and `DS_MCP_DIR` at `windows-deploy.sh:241`/`:258`, the pin barriers at `desktop-publish-windows.sh:67` (12 calls over six repos, asserted at `src/tests/release-coordination.test.ts:316`), `WINDOWS_REPOS` at `windows-deploy.sh:162`, `PUBLISH_REPOS` at `desktop-publish-linux.sh:60`, `RELEASE_REPOS` at `linux-deploy.sh:83`.\n\n**Must be proved before anything is deleted:**\n\n1. **`ds` answers the identity contract the sidecar preparer asserts.** `prepare-ds-mcp-sidecars.sh:189` requires a `ds-mcp-build/v1` document with a `source_sha`. `ds version --output json` must carry the same field before the preparer is rewritten, or the release loses its provenance check. Proved by the first slice (`ds version` already exists) plus a `ds`-shaped assertion replacing `test-prepare-ds-mcp-sidecars.sh:29`.\n2. **The `ds-mcp-canary` release gate must be resolved by decision, not rename.** `scripts/release/edge-readiness-preflight.mjs:24` lists ds-mcp as a mandatory clean `run` checkout and `:34` probes a live `ds-mcp-canary` Cloud Run revision — but **no Terraform in any of these repos creates that service**; the only hosted trace is one `roles/run.invoker` grant at `ds-apis-tf/edge_runtime_permissions.tf:63`. Either keep the service name or drop the gate (and `:653`, and the fixtures at `edge-readiness-preflight.test.mjs:83`). This blocks the Go path deletion.\n3. **The headless smoke must stop speaking MCP.** `scripts/desktop/headless-semantic-smoke.sh:148/:328/:415/:424` requires ds-mcp Go sources, builds them, runs `desktop-semantic-smoke --mcp-bin ds-mcp` and asserts protocol `2026-07-28` and server `{Data Solutions,1.0.0}`. It has to be rewritten as an ordinary `ds` invocation against the live bridge descriptor before the Go build can go.\n4. **The shipped pairing prompt is user-visible text.** `src/lib/desktop/agent-prompt.ts:9` tells agents to run `ds-mcp build-info` and `mcp --stdio` with `DS_GRID_MCP_BIN`, and `src/tests/desktop-agent-prompt.test.ts:12` asserts those exact strings. It must be rewritten to say \"run `ds --help`\" in the same commit as the sidecar change, or paired agents break in the field.\n5. **Sidecar deadlines are currently derived from ds-mcp's.** `src-tauri/src/agent_bridge.rs:32` sets a 31-minute invocation timeout explicitly to sit above ds-mcp's 30-minute tool deadline, and `src-tauri/src/network_reporter.rs:49` sets the reporter child deadline below ds-mcp's 15-minute public deadline (asserted at `:5197`). `ds` must publish its own deadlines before those constants are re-derived.\n6. **`ds-report` stays.** It is not deletable at all: it is the reporter's typed process boundary and `ds report *` invokes it. Only the `ds-mcp`→`ds-report` hop disappears.\n7. **`DS_MCP_LOCAL_PRINCIPAL` / `DS_MCP_LOCAL_PROJECT`** are asserted as documentation strings by `src/tests/feedback-agent-bridge.test.ts:67` against `docs/UI-TODOS.md:135`. Their replacement in `ds desktop status` must land before that ledger row is edited.\n\n## Next slices\n\nThe first slice is already in the tree: `network.inspect` (`crates/ds-cli-network/src/inspect.rs`, `Effect::Discovery`, `Authority::None`) and `desktop.status` (`crates/ds-cli-desktop/src/status.rs`, same). Between them they prove linking the authoritative ds-network crates directly and discovering the paired-desktop bridge without holding a credential.\n\n**Slice 2 — `ds report engine`.** One command, one crate: invoke the `ds-report` sidecar's `build-info` and `task-schemas`, at `authority=none`, `effect=discovery`, with a typed `unavailable` outcome when the sidecar is absent. It is the cheapest command that exercises the *other* integration route — a versioned native process contract rather than a linked crate — and unlike `solar verify-weather` it needs no new path dependency (the workspace `Cargo.toml` does not yet carry ds-solar).\n*Unlocks for deletion:* `ds-report build-info` and `ds-report task-schemas` as operator-facing doors (`ds-network-reporter/src/bin/ds-report.rs:267`, `:272`) together with the MCP tool-parity check `task_schemas` (`src/tasks.rs:296`) exists to serve, and `report_engine_identity` (`src/tasks.rs:70`) as a separate discovery surface. It also proves the sidecar identity handshake, which is precondition (1) above for rewriting `scripts/desktop/prepare-ds-mcp-sidecars.sh:189`.\n\n**Slice 3 — `ds desktop state`.** One command over the bridge transport: `desktop_get_app_state` at `desktop_pairing`. It proves that `ds` can reuse the paired app's authority over loopback without ever holding the JWT — the precondition for every `map`, `design`, `solar workspace` and `report … export` command in the tree.\n*Unlocks for deletion:* `desktop_get_app_state` (`ds-mcp internal/tools/tools.go:565`) and, with it, the entire `mcp --stdio` initialize path that `ds desktop status` currently replaces by discovery — meaning `src/lib/desktop/agent-prompt.ts:9` and `src/tests/desktop-agent-prompt.test.ts:12` can be rewritten (precondition 4), and `scripts/desktop/headless-semantic-smoke.sh:415` can drop `--mcp-bin ds-mcp` for a plain `ds` invocation (precondition 3).\n\n**Slice 4 — the five PLS file tasks: `ds pls pole-capacity read|assign`, `ds pls section-orientation`, `ds pls reference-closure`, `ds pls compare-don`.** Linked `ds-grid-tasks` over `ds-io`, all `authority=none`, results on stdout under the output envelope instead of a `--result FILE` written with `create_new`. This is the largest single collapse of authority in the tree — five capabilities that carry `desktop_pairing` today purely because ds-mcp reached them through a process boundary — and it wires `compare_don_assignment` for the first time.\n*Unlocks for deletion:* `ds-mcp internal/tools/pole_capacity.go`, `section_orientation.go`, `reference_closure.go` and `don_assignment.go` in full, plus the never-assigned `DonAssignmentTasks` wiring gap at `cmd/ds-mcp/main.go:257`; the `--result FILE` create_new / RAII-delete file-task protocol in `crates/ds-grid-cli/src/main.rs:728-777`; and `DS_GRID_MCP_BIN` together with `ds_grid_mcp_prepare` (`ds-web/scripts/run/services.sh:246`, `:299`), which is the last thing keeping the `ds-grid-mcp` binary alive once `ds network describe` follows.",
    "judgement": {
      "winner": "grouping-c",
      "scores": [
        {
          "key": "grouping-a",
          "discoverability": 5,
          "stability": 6,
          "vocabulary": 6,
          "rationale": "Discoverability is the weak leg, and A knows it: it spends a whole note defending the `network inspect` (--model) vs `exchange inspect` (--source) collision, which is a two-command name clash surviving at the top of two different domains. Worse, PLS-native file work is split three ways — `exchange structure ingest/emit/roundtrip` in one domain, `pls audit-structures`/`pls parse-usage`/`pls available-structures` in another, `exchange template export-pls` in a third place — so \"ingest a PLS structure\" and \"convert a PLS backup\" both cost a wrong-guess call before the right domain, i.e. 4+ calls from root. Stability is middling: the network/exchange/pls boundary is fuzzy enough that every new file-format or PLS-native capability re-litigates its home, and nesting design under `network` (16 leaves already, with MV cutover live in the workspace per MV_CUTOVER_START_HERE.md) sets up a later promotion to its own domain — exactly the reshuffle the criterion asks about. Vocabulary is fair but not clean: `exchange` is a crate name (ds-grid-exchange), not a word a line engineer says — they say convert — and A truncates real terms (`pls parse-usage`, `pls audit-structures`, `network deflections`) and reorders `compare-don-assignment` into `pls don compare`. Its genuine strength, undersold in its own notes, is that it is the only tree whose every domain index stays small enough for the 900 B tier-2 budget."
        },
        {
          "key": "grouping-b",
          "discoverability": 6,
          "stability": 5,
          "vocabulary": 7,
          "rationale": "Stability is the decisive failure, and it is structural rather than incidental: B makes authority the domain axis while authority classifications are actively being corrected — B's own notes list five corrections (QGIS drops to none, DS_SOLAR_PLACEMENT stops being an authority input, collisions becomes artifact_write, design.save gains --yes, feedback's union authority). An axis whose values you are still fixing is not a foundation. Concretely, the governing prompt contemplates a headless login for CI; when it lands, the desktop_pairing tier partly dissolves and `design` (uniformly desktop_user) loses its whole stated reason for existing apart from `network`. Discoverability suffers from the same lens: an agent that wants a transformer report must choose between `report transformer build` and `report transformer export` at the leaf, and neither root nor domain help says which lane its authority permits — that is a 4-call path. Survey work is buried at `ds project survey dashboard` under an 18-command grab-bag cohered only by authority. A standalone `qgis` domain spends a permanent root line on four commands, against CLAUDE.md's explicit \"nest instead of proliferating\" rule, and `network` at 27 leaves overruns the tier-2 budget. Vocabulary is decent — it keeps convert/compose/check/library/template/oracle/structure — but it drops `map` and `survey` as top-level words users actually say, shortens `pole-capacity` to `capacity`, and inverts `inspect-pls-reference-closure` into `pls closure inspect`."
        },
        {
          "key": "grouping-c",
          "discoverability": 9,
          "stability": 8,
          "vocabulary": 9,
          "rationale": "Best on all three, and the only proposal that states a discoverability *rule* rather than asserting an outcome: two-token group nodes exist only where a group has two or more children, so no group ever costs a call to reach a single leaf (hence `ds pls section-orientation` flat, `ds pls oracle submit` nested). Walk the real cases from root: convert a PLS backup (3), ingest a structure (3), export a transformer report (3), read survey data (3), edit LV features (3), find geometry outliers (3, flattened to `map outliers`). Only QGIS misses — `desktop`'s root line never says QGIS — which is the single graft it needs. Stability is high because the axis is subject, and subjects here are the product's own long-lived areas; the task ORDER is presentational, so adding a domain moves a print position, not a tree. `design` as a domain is right with MV cutover in flight, where A's `network design` would need promoting later. Vocabulary is the strongest of the three and essentially inventive-free: it keeps pole-capacity, section-orientation, reference-closure, compare-don, structure ingest/emit/roundtrip/audit/usage/available, convert, compose, check, library, template, oracle, forms, map, survey, design, report — matching ds-grid-cli's live subcommand names and ds-mcp's tool names. Its one deliberate rename, splitting project-scoped `survey forms *` from cross-project `survey schema *`, is a safety improvement with a stated reason: `commit_form_definition` writes a GLOBAL schema across every project and should not sit one word away from the project-scoped commit. Marks off for `report tiles` (a map artifact filed under deliverables), for keeping the QGIS commands at desktop_pairing when they are local-filesystem work, for over-marking solar as desktop_user, and for a 28-leaf `network` index that overruns the tier-2 byte budget."
        }
      ],
      "grafts": [
        "From B: split `report transformer build` (authority=none, local canonical bytes, the ds-report sidecar) from `report transformer export` (desktop_user, reads the paired app's rooms), and the same for `combined`. C collapses both into one desktop_user `report.transformer` and silently loses the offline lane — the lane that needs no app running at all.",
        "From B: adopt tree-wide the rule that where a subject crosses an authority tier, the crossing is a visible nesting node or a distinct verb, never a hidden flag. This directly overrules C's own note #5, which makes the solar cloud lane a `--lane` flag on `solar run start` and `solar report`; under B's rule those become distinct commands, which is right because the cloud lane is project-authority artifact_write and the local lane is authority=none.",
        "From B: correct the QGIS authority to `none`. The four qgis_* commands probe the local filesystem and manage a confined DS profile with no session involved; the `desktop_pairing` they carry today is an artifact of ds-mcp reaching them over stdio. C keeps desktop_pairing on all four, which would make `ds qgis discover` refuse on a machine with no app running — for a command whose entire job is to answer before anything is installed.",
        "From B (A agrees): take the solar authority audit. All twelve solar tools declare AuthLocal today and are mutated to AuthDesktopUser when DS_SOLAR_PLACEMENT=paired, so their effective authority is an environment variable. Only `solar workspace choose` genuinely needs the desktop, for the native picker and the grant. C marks run progress/result, result read, and document list/read as desktop_user, preserving exactly the artifact B deletes.",
        "From B: use `report engine` as the near-term second proof rather than C's `solar verify-weather`. It is the cheapest command that exercises the *other* integration route — a versioned native process contract (`ds-report build-info` + `task-schemas`) with a typed unavailable outcome when the sidecar is absent — at authority=none with no effect. C's choice needs a ds-solar path dependency the workspace Cargo.toml does not yet carry, so it cannot be the first thing proved.",
        "From A: move QGIS under `map`, not `desktop`. A's `map qgis discover|status|plan|apply` sits in a domain whose root line already says GIS, so the word is reachable from tier 1; C's `desktop` line (\"the paired DS GridDesign session and local environment\") never says QGIS, which is C's only 4-call case. This also avoids B's standalone `qgis` domain, which spends a permanent root line on four commands against CLAUDE.md:33.",
        "From A: flatten `network session apply` to `network apply`. `session` has exactly one child, so C's own stated singleton rule — group nodes only where a group has two or more children — requires A's flattening. C left it nested purely by inheritance from the ds-grid `session apply` spelling.",
        "From A: keep the inspect-disambiguation discipline even though the collision is gone. A had to write a whole note defending `network inspect` (--model, what is in this model) against `exchange inspect` (--source, what can these files become); C dissolves it by nesting the second as `network convert inspect`, but A's requirement survives, because `ds capabilities --search \"inspect\"` returns them adjacent. Both first help lines must state model-identity versus source-classification explicitly.",
        "From A: take the domain-index size constraint, not the domain split that motivated it. `help::domain` in crates/ds-cli-contract/src/help.rs:78 prints one line per leaf with the full sub-path joined, so C's 28-leaf `network` overruns the 900 B tier-2 budget in crates/ds/tests/context_budget.rs (today 263-270 B). A is the only tree that keeps every index small. Resolve it as a rendering rule — collapse a multi-child group to one index row, e.g. `convert  (inspect, plan, run)` — or raise the budget in the same commit with the reason, per CLAUDE.md's rule against silent relaxation.",
        "From B: keep the authority lens as a query rather than a tree. Effect and Authority are already declared fields on every Command in crates/ds-cli-contract/src/spec.rs, and `ds capabilities` already supports --search, so adding `--authority none` / `--effect global_write` filters delivers B's entire organising insight — 'show me everything that runs offline', 'show me everything that mutates shared state' — for zero root-help bytes and zero tree churn. This is the synthesis: C's subject tree plus B's lens as a projection over it.",
        "From B: prefer `design gaps` to C's `design coverage` for query_surveyed_not_designed. `gaps` is the noun the capability describes (surveyed areas not yet designed); `coverage` is invented and reads like a metric. Keep C's placement in `design` over A's placement in `survey` — the question is a design-completeness question, not a survey-data question."
      ]
    },
    "inventoryCounts": [
      {
        "source": "/home/magese/data-solutions/ds-mcp/internal/tools/*.go (registration site: internal/tools/tools.go:718-724; wiring: cmd/ds-mcp/main.go:257, cmd/ds-mcp/solar.go:249)",
        "entries": 65
      },
      {
        "source": "/home/magese/data-solutions/ds-network/crates/ds-grid-cli/src/main.rs run() match at line 213 (+ nested dispatchers describe_command:330, library_command:3154, template_command:3275, session_command:3097, seed_command:3534, and pls_postprocess::command at crates/ds-grid-cli/src/pls_postprocess.rs:311)",
        "entries": 45
      },
      {
        "source": "/home/magese/data-solutions/ds-web/src-tauri/src/agent_bridge.rs:37-83 (LOCAL_OPERATIONS) cross-referenced with /home/magese/data-solutions/ds-web/src/lib/desktop/agent-bridge.ts (CAPABILITIES 272-950, agentCapabilities 905-952, executeAgentOperation 1050-1168, handler bodies 1170-2810)",
        "entries": 42
      },
      {
        "source": "/home/magese/data-solutions/ds-solar (workspace: crates/ds-solar-{contracts,engine,report,runtime}, apps/ds-solar-{cli,cloud}) and /home/magese/data-solutions/ds-network-reporter (root crate + crates/governance, crates/canvas-print excluded from the deploy workspace), cross-checked against /home/magese/data-solutions/ds-web/src-tauri/Cargo.toml and src/",
        "entries": 55
      },
      {
        "source": "Manual inventory of ds-web, ds-deploy, ds-apis-tf, ds-sre, ds-system (node_modules/target/.git/dist/build excluded)",
        "entries": 94
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Inventory"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Classify"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "inventory:ds-mcp-tools",
      "phaseIndex": 1,
      "phaseTitle": "Inventory",
      "agentId": "a6c91305aaeaa145b",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787336505596,
      "queuedAt": 1787336505582,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "/home/magese/data-solutions/ds-mcp/internal/tools/*.go (reg…",
      "promptPreview": "Read /home/magese/data-solutions/ds-mcp/internal/tools/*.go (skip _test.go files). Every tool is declared as a Go struct literal containing \"Descriptor: Descriptor{ Name: ..., Effect: ..., Auth: ..., Execution: ... }\". Extract EVERY registered tool. For each, record its Name verbatim, the file:line, its Effect and Auth constants translated to the token vocabulary (EffectReadOnly -> read_only, Auth…",
      "lastProgressAt": 1787336821452,
      "tokens": 109889,
      "toolCalls": 37,
      "durationMs": 315854,
      "resultPreview": "{\"source\":\"/home/magese/data-solutions/ds-mcp/internal/tools/*.go (registration site: internal/tools/tools.go:718-724; wiring: cmd/ds-mcp/main.go:257, cmd/ds-mcp/solar.go:249)\",\"entries\":[{\"name\":\"describe_data_solutions_tools\",\"where\":\"internal/tools/tools.go:235\",\"owner\":\"ds-mcp (its own registry projection)\",\"effect\":\"discovery\",\"authority\":\"none\",\"transport\":\"chat\",\"real\":false,\"summary\":\"List…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "inventory:ds-grid-cli-subcommands",
      "phaseIndex": 1,
      "phaseTitle": "Inventory",
      "agentId": "a3a285a5e673cf906",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787336505597,
      "queuedAt": 1787336505582,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "seed_command has NO nested subcommands - it is a single fla…",
      "promptPreview": "Read /home/magese/data-solutions/ds-network/crates/ds-grid-cli/src/main.rs. Its run() function has a match on the first argv token listing every subcommand. Extract EVERY subcommand (including the nested ones inside session_command, library_command, template_command, seed_command - read those functions to find their sub-subcommands). For each record: name verbatim, file:line of its handler, which …",
      "lastProgressAt": 1787336720586,
      "tokens": 78850,
      "toolCalls": 26,
      "durationMs": 212385,
      "resultPreview": "{\"entries\":[{\"name\":\"describe commands\",\"where\":\"crates/ds-grid-cli/src/main.rs:342\",\"owner\":\"ds-grid-engine\",\"effect\":\"discovery\",\"authority\":\"none\",\"transport\":\"argv\",\"real\":true,\"summary\":\"discovery: dumps ds-grid-engine command descriptors verbatim (JSON or table)\"},{\"name\":\"describe operations\",\"where\":\"crates/ds-grid-cli/src/main.rs:343\",\"owner\":\"ds-grid-engine\",\"effect\":\"discovery\",\"authori…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "inventory:desktop-bridge-ops",
      "phaseIndex": 1,
      "phaseTitle": "Inventory",
      "agentId": "a5619bb9345438350",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787336505597,
      "queuedAt": 1787336505582,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Five of the 42 allowlisted operations (gis.clip, style.prev…",
      "promptPreview": "Read /home/magese/data-solutions/ds-web/src-tauri/src/agent_bridge.rs - it has a LOCAL_OPERATIONS const array listing every allowed semantic operation. Then read /home/magese/data-solutions/ds-web/src/lib/desktop/agent-bridge.ts to find each operation's actual implementation and what it does. Extract EVERY operation. For each: name verbatim (e.g. \"solar.run.start\"), file:line in agent-bridge.ts wh…",
      "lastProgressAt": 1787336769729,
      "tokens": 96226,
      "toolCalls": 23,
      "durationMs": 261527,
      "resultPreview": "{\"entries\":[{\"name\":\"folder.connect\",\"where\":\"src/lib/desktop/agent-bridge.ts:1117\",\"owner\":\"ds-web frontend\",\"effect\":\"local_ui\",\"authority\":\"desktop_pairing\",\"transport\":\"bridge_op\",\"real\":true,\"summary\":\"Native directory picker grants a session-scoped read grant on one folder\"},{\"name\":\"files.connect\",\"where\":\"src/lib/desktop/agent-bridge.ts:1119\",\"owner\":\"ds-web frontend\",\"effect\":\"local_ui\",\"…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "inventory:solar-and-reporter",
      "phaseIndex": 1,
      "phaseTitle": "Inventory",
      "agentId": "a5b0d5fc6f022bb4c",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787336505597,
      "queuedAt": 1787336505582,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Both workspaces already ship their own CLI binary: ds-solar…",
      "promptPreview": "Inspect /home/magese/data-solutions/ds-solar (read Cargo.toml, crates/*/Cargo.toml, and any CLI or public entry points) and /home/magese/data-solutions/ds-network-reporter (same). Identify the PUBLIC operation surfaces each workspace exposes that a CLI could legitimately call: named Rust functions or binaries that are the authoritative entry points for solar runs, prepared batches, document compos…",
      "lastProgressAt": 1787336821099,
      "tokens": 103600,
      "toolCalls": 39,
      "durationMs": 312897,
      "resultPreview": "{\"entries\":[{\"name\":\"ds-solar\",\"where\":\"ds-solar/apps/ds-solar-cli/src/main.rs:160\",\"owner\":\"ds-solar/apps/ds-solar-cli\",\"effect\":\"local_file_write\",\"authority\":\"none\",\"transport\":\"argv\",\"real\":true,\"summary\":\"Clap CLI binary: prepare/run/request/verify-weather/parity/compare/bench subcommands\",\"transport_note\":null},{\"name\":\"ds-solar prepare\",\"where\":\"ds-solar/apps/ds-solar-cli/src/main.rs:186\",\"…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "inventory:deployment-wiring",
      "phaseIndex": 1,
      "phaseTitle": "Inventory",
      "agentId": "ab38e416741f3603d",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787336505598,
      "queuedAt": 1787336505582,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Manual inventory of ds-web, ds-deploy, ds-apis-tf, ds-sre, …",
      "promptPreview": "Find every SHIPPING reference to the Go/MCP path that a migration to a Rust \"ds\" CLI would have to replace. Search /home/magese/data-solutions/ds-web, /home/magese/data-solutions/ds-deploy, /home/magese/data-solutions/ds-apis-tf, /home/magese/data-solutions/ds-sre, /home/magese/data-solutions/ds-system for: go build / go.mod checks, ds-mcp and ds-grid-mcp sidecar builders, Tauri externalBin declar…",
      "lastProgressAt": 1787337469748,
      "tokens": 177997,
      "toolCalls": 78,
      "durationMs": 961546,
      "resultPreview": "{\"source\":\"Manual inventory of ds-web, ds-deploy, ds-apis-tf, ds-sre, ds-system (node_modules/target/.git/dist/build excluded)\",\"entries\":[{\"name\":\"ds-mcp sidecar Go build\",\"where\":\"scripts/desktop/prepare-ds-mcp-sidecars.sh:150\",\"owner\":\"ds-web\",\"effect\":\"unknown\",\"authority\":\"unknown\",\"transport\":\"build_step\",\"real\":true,\"summary\":\"CGO_ENABLED=0 go build -trimpath -buildvcs of ./cmd/ds-mcp; beco…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "group:grouping-a",
      "phaseIndex": 2,
      "phaseTitle": "Classify",
      "agentId": "a61e97e0d0c4ae036",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787337469767,
      "queuedAt": 1787337469754,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DOMAIN COUNT AND ROOT HELP. Nine domains, printed in this o…",
      "promptPreview": "You are designing the domain tree for a single Rust CLI named \"ds\" that is the one door into the Data Solutions geospatial/power-network stack, for both humans and coding agents.\n\nHARD CONSTRAINTS:\n- ONE binary. Everything below lives in it, including PLS-CADD and PLS exchange.\n- Discovery is TIERED: \"ds --help\" prints ONLY domain one-liners. \"ds <domain> --help\" prints only that domain's command …",
      "lastProgressAt": 1787338067881,
      "tokens": 101046,
      "toolCalls": 10,
      "durationMs": 598113,
      "resultPreview": "{\"domains\":[{\"id\":\"network\",\"summary\":\"The canonical grid model: identity, checks and LV/MV design.\",\"rationale\":\"A power-line engineer calls all of this \\\"the network model\\\" — the authored .dsgrid model and the LV/MV transformer rooms being designed against it are the same subject, and both are computed by ds-network crates. Reading, validating, checking and editing the model belong together; tu…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "group:grouping-b",
      "phaseIndex": 2,
      "phaseTitle": "Classify",
      "agentId": "a8a4a0e64b87fec53",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787337469767,
      "queuedAt": 1787337469754,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "LENS. Domains are drawn so each has one authority story, an…",
      "promptPreview": "You are designing the domain tree for a single Rust CLI named \"ds\" that is the one door into the Data Solutions geospatial/power-network stack, for both humans and coding agents.\n\nHARD CONSTRAINTS:\n- ONE binary. Everything below lives in it, including PLS-CADD and PLS exchange.\n- Discovery is TIERED: \"ds --help\" prints ONLY domain one-liners. \"ds <domain> --help\" prints only that domain's command …",
      "lastProgressAt": 1787338319641,
      "tokens": 140933,
      "toolCalls": 11,
      "durationMs": 846690,
      "resultPreview": "{\"domains\":[{\"id\":\"network\",\"summary\":\"Canonical .dsgrid models: identify, check, convert, package.\",\"rationale\":\"Everything here runs offline with no principal at all: it reads and writes files you already possess, through the ds-grid crates the desktop already links. One authority story (none) and a visible effect ladder from discovery to local_file_write, so nothing in this domain can touch sha…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "group:grouping-c",
      "phaseIndex": 2,
      "phaseTitle": "Classify",
      "agentId": "a8623a1222eb3974b",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787337469767,
      "queuedAt": 1787337469754,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "LENS AND THE ONE COMPROMISE. A pure task lens (import / ins…",
      "promptPreview": "You are designing the domain tree for a single Rust CLI named \"ds\" that is the one door into the Data Solutions geospatial/power-network stack, for both humans and coding agents.\n\nHARD CONSTRAINTS:\n- ONE binary. Everything below lives in it, including PLS-CADD and PLS exchange.\n- Discovery is TIERED: \"ds --help\" prints ONLY domain one-liners. \"ds <domain> --help\" prints only that domain's command …",
      "lastProgressAt": 1787338066236,
      "tokens": 112988,
      "toolCalls": 9,
      "durationMs": 593284,
      "resultPreview": "{\"domains\":[{\"id\":\"project\",\"summary\":\"Bind the work: projects, tasks, feedback, admin units.\",\"rationale\":\"The first thing a person or agent does is establish which project they are in and what work is outstanding; every paired or authenticated command downstream refuses without it. Task roster, obstacle ledger and administrative reference names all answer \\\"what am I working on and who else is a…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "judge:domain-trees",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "aa209f13b8063277b",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787338319660,
      "queuedAt": 1787338319648,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "grouping-c",
      "promptPreview": "Three independent domain trees were proposed for the single \"ds\" CLI. Score each on discoverability (can an agent reach the right command in at most 3 calls, starting from \"ds --help\"), stability (does adding a new capability later force a reshuffle), and vocabulary (does it keep words existing Data Solutions users already use). Then pick a winner and list specific ideas from the other two worth g…",
      "lastProgressAt": 1787338543578,
      "tokens": 110486,
      "toolCalls": 12,
      "durationMs": 223918,
      "resultPreview": "{\"winner\":\"grouping-c\",\"scores\":[{\"key\":\"grouping-a\",\"discoverability\":5,\"stability\":6,\"vocabulary\":6,\"rationale\":\"Discoverability is the weak leg, and A knows it: it spends a whole note defending the `network inspect` (--model) vs `exchange inspect` (--source) collision, which is a two-command name clash surviving at the top of two different domains. Worse, PLS-native file work is split three way…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "synthesize:final",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "ae3e81e0664376312",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1787338543580,
      "queuedAt": 1787338543580,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cat > /tmp/claude-1000/-home-magese-data-solutions-ds-cli/4…",
      "promptPreview": "Produce the FINAL domain tree and migration matrix for the single \"ds\" Rust CLI.\n\nBase it on the winning proposal, grafting in the improvements named by the judge.\n\nJudge verdict: {\"winner\":\"grouping-c\",\"scores\":[{\"key\":\"grouping-a\",\"discoverability\":5,\"stability\":6,\"vocabulary\":6,\"rationale\":\"Discoverability is the weak leg, and A knows it: it spends a whole note defending the `network inspect` (…",
      "lastProgressAt": 1787338965048,
      "tokens": 135642,
      "toolCalls": 8,
      "durationMs": 421467,
      "resultPreview": "## Domain tree\n\nNine domains, printed in this order. Deepest path is three tokens past `ds`. Two-token group nodes exist only where a group has two or more children (C's singleton rule), so `network apply`, `pls section-orientation`, `map tiles`, `report bundle` and `report collisions` are flat.\n\n```\nds project   — bind the work\n  project list                    List your projects for one lifecycl…"
    }
  ],
  "totalTokens": 1167657,
  "totalToolCalls": 253
}