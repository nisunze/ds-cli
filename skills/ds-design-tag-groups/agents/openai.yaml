interface:
  display_name: "DS Design Tag Groups"
  short_description: "Batch eligible project tags and export an explicit grouping projection"
  default_prompt: "Use $ds-design-tag-groups to batch-assign an explicitly selected project tag definition, or export a digest-pinned projection for ordered definition ids, through ds design group."
