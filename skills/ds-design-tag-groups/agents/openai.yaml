interface:
  display_name: "DS Design Tag Groups"
  short_description: "Batch a project's governed city/phasing tags and export the report projection"
  default_prompt: "Use $ds-design-tag-groups to assign a city or delivery phase across transformers, or to export the tag document a per-city report groups by, through ds design group."
