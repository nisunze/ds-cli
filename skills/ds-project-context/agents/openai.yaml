interface:
  display_name: "DS Project Context"
  short_description: "Select a project and work with its assets"
  default_prompt: "Use $ds-project-context to discover or switch the active DS project, then work with the relevant project-scoped assets."
