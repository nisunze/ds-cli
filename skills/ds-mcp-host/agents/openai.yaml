interface:
  display_name: "DS through MCP"
  short_description: "Install compact or typed ds MCP views"
  default_prompt: "Use $ds-mcp-host to install the broad chapter server or one typed workflow profile, then route through live ds contracts."
