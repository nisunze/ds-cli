[package]
name = "ds-cli-solar"
description = "The `ds solar` domain, over the `ds-solar` process contract owned by the ds-solar workspace."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
base64 = { workspace = true }
ds-cli-auth = { workspace = true }
ds-cli-contract = { workspace = true }
# The paired desktop owns selected-project state, cache-first city capture and
# any authenticated refresh. The CLI invokes only fixed semantic operations;
# a JWT never crosses back.
ds-cli-desktop = { workspace = true }
ds-cli-exec = { workspace = true }
ds-cli-report = { workspace = true }
ds-command-kernel = { workspace = true }
libc = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
tempfile = "3"
fs2 = "0.4"
