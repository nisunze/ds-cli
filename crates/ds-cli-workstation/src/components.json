[
  {
    "id": "libreoffice",
    "required": true,
    "purpose": "Edit delivered documents and perform governed headless report conversion.",
    "provenance": "The Document Foundation official distribution",
    "linux_executables": [
      "libreoffice",
      "soffice"
    ],
    "macos_executables": [
      "libreoffice",
      "soffice"
    ],
    "windows_executables": [
      "soffice.exe",
      "libreoffice.exe"
    ],
    "linux_plan": [
      "Use the distribution package manager and its signed repository metadata.",
      "Verify the executable version and a harmless task-owned headless conversion."
    ],
    "macos_plan": [
      "Use the conventional trusted package mechanism or The Document Foundation official distribution.",
      "Verify the executable version and a harmless task-owned headless conversion."
    ],
    "windows_plan": [
      "Prefer the platform package manager and The Document Foundation package identity.",
      "Only after a verified package-manager transport failure, resolve an installer through the official Metalink response.",
      "Require the selected URL to be listed by that Metalink and require the published SHA-256 to match before execution.",
      "Verify package registration, executable version, a harmless task-owned headless conversion, and exact cleanup; record task ownership only for installations made by this command."
    ]
  },
  {
    "id": "git-bash",
    "required": false,
    "purpose": "Provide the Git for Windows Bash environment when a Windows workflow explicitly needs it.",
    "provenance": "Git for Windows official distribution or the platform's trusted package catalog",
    "linux_executables": [],
    "macos_executables": [],
    "windows_executables": [
      "bash.exe"
    ],
    "linux_plan": [
      "Use the native Bash or selected login shell; Git Bash is a Windows product and is not emulated here."
    ],
    "macos_plan": [
      "Use the native shell and conventional package manager; Git Bash is a Windows product and is not emulated here."
    ],
    "windows_plan": [
      "Detect a suitable existing Git for Windows Bash first and leave it untouched when present.",
      "Acquisition is planning-only until its exact lifecycle is proven; VS Code configuration is a separate implemented intent."
    ]
  },
  {
    "id": "rwanda-reference",
    "required": false,
    "purpose": "Provide versioned Rwanda villages and related reference data with provenance and content receipts.",
    "provenance": "A DS-governed component receipt naming the upstream source, version, and file SHA-256 values",
    "version": "NISR-village-boundary-2022",
    "license": "NISR Open Data designation; attribution: Village boundary data produced by NISR in 2022",
    "source": "https://gis-server.statistics.gov.rw/server/rest/services/Hosted/Village_Boundary_2022/FeatureServer/3",
    "linux_executables": [],
    "macos_executables": [],
    "windows_executables": [],
    "linux_plan": [
      "After explicit confirmation, acquire the fixed official NISR Village Boundary 2022 Open Data layer in bounded, low-bandwidth-tolerant pages without geometry simplification.",
      "Persist source-precision GeoJSON plus provenance, version, license, ownership, and SHA-256 receipt; re-verify before returning."
    ],
    "macos_plan": [
      "After explicit confirmation, acquire the fixed official NISR Village Boundary 2022 Open Data layer in bounded, low-bandwidth-tolerant pages without geometry simplification.",
      "Persist source-precision GeoJSON plus provenance, version, license, ownership, and SHA-256 receipt; re-verify before returning."
    ],
    "windows_plan": [
      "After explicit confirmation, acquire the fixed official NISR Village Boundary 2022 Open Data layer in bounded, low-bandwidth-tolerant pages without geometry simplification.",
      "Persist source-precision GeoJSON plus provenance, version, license, ownership, and SHA-256 receipt; re-verify before returning."
    ]
  },
  {
    "id": "tippecanoe",
    "required": false,
    "purpose": "Build vector tiles locally on Linux with the shared kernel-pinned Tippecanoe and PMTiles toolchain.",
    "provenance": "Pinned official felt/tippecanoe source and protomaps/go-pmtiles release with SHA-256 verification",
    "linux_executables": [
      "tippecanoe",
      "tile-join"
    ],
    "macos_executables": [
      "tippecanoe",
      "tile-join"
    ],
    "windows_executables": [],
    "linux_plan": [
      "Detect an existing tippecanoe first and leave a working installation untouched.",
      "Build the pinned official Tippecanoe source and download the pinned official PMTiles release after explicit interactive approval.",
      "Install them as shared host tools, then verify both exact versions before recording the receipt."
    ],
    "macos_plan": [
      "Detect an existing tippecanoe first and leave a working installation untouched.",
      "Install through the conventional trusted package mechanism and verify the executable version."
    ],
    "windows_plan": [
      "Not applicable: tippecanoe has no governed Windows distribution, so local tiling is a Linux capability and Windows continues to use the deployed tiler."
    ]
  },
  {
    "id": "pandoc",
    "required": false,
    "purpose": "Convert delivered documents locally so report finishing does not depend on a network service.",
    "provenance": "The distribution's signed package catalog, or the official jgm/pandoc release",
    "linux_executables": [
      "pandoc"
    ],
    "macos_executables": [
      "pandoc"
    ],
    "windows_executables": [
      "pandoc.exe"
    ],
    "linux_plan": [
      "Detect an existing pandoc first and leave a working installation untouched.",
      "Install from the distribution package manager using its signed repository metadata, non-interactively.",
      "Verify by running the installed executable's own version report before recording the receipt."
    ],
    "macos_plan": [
      "Detect an existing pandoc first and leave a working installation untouched.",
      "Install through the conventional trusted package mechanism and verify the executable version."
    ],
    "windows_plan": [
      "Detect an existing pandoc first and leave a working installation untouched.",
      "Prefer the proven platform package manager and the official pandoc package identity.",
      "Verify the executable version before recording the receipt."
    ]
  }
]
