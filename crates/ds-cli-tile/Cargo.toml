[package]
name = "ds-cli-tile"
description = "The `ds tile` domain: a project's vector-tile outputs — status, preflight, plan, generate, list, add, remove."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
ds-cli-auth = { workspace = true }
ds-cli-contract = { workspace = true }
# Managed output status, preflight and generation use the fixed native client.
# The tile catalogue remains paired until its public contracts are extracted.
ds-cli-desktop = { workspace = true }
serde_json = { workspace = true }
