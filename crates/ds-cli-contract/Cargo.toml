[package]
name = "ds-cli-contract"
description = "The `ds` command-line contract: command metadata, tiered help, output envelope, error classes, exit codes, and argument parsing. Domain-free by construction."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
