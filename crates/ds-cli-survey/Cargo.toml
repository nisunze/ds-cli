[package]
name = "ds-cli-survey"
description = "The `ds survey` domain: survey forms and their data — schema, fields, export and the Working Area — through the paired application."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
ds-cli-contract = { workspace = true }
# Forms are governed Form Factory documents behind ds-brain, and survey data
# is reachable only under the signed-in session. Every command here is one
# named bridge operation the paired application performs; nothing is linked.
ds-cli-desktop = { workspace = true }
serde_json = { workspace = true }
