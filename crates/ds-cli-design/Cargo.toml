[package]
name = "ds-cli-design"
description = "The `ds design` domain: offline LV compute and governed design collaboration."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
ds-cli-contract = { workspace = true }
ds-cli-auth = { workspace = true }
# Collaboration commands route through the paired application's project
# session. Offline Fast LV compute links the same pure ds-network kernel the
# browser's WASM adapter calls; the CLI adds no engineering logic.
ds-cli-desktop = { workspace = true }
ds-geo = { workspace = true }
ds-network = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
