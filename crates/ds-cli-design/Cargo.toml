[package]
name = "ds-cli-design"
description = "The `ds design` domain: saved Transformer Status selections, versioned attachments, and project tags and comments."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
ds-cli-contract = { workspace = true }
# Design collaboration is governed shared state behind ds-brain. The paired
# application holds the session that may reach it, so every command here is one
# named bridge operation and there is no engine to link.
ds-cli-desktop = { workspace = true }
serde_json = { workspace = true }
