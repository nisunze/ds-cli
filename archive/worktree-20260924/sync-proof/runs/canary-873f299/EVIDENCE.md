# Sync-up proof -- canary-873f299

Binary: `/usr/bin/ds-canary`  
Build: version=0.1.0 source_sha=873f29936d368ed6bcfa45275fbc304208cb90a1 ds_network_source_sha=10936d574e5f79e6c648941b2c6573061d22b745 profile=release target=x86_64-unknown-linux-gnu dirty=False

Lane: canary. Project under test: `agct38sq_sample`. Second project attempted: `arjgpydw_survey_test`.

## Invariant table

| # | Invariant | Status | Command(s) | Raw file(s) | What the output literally showed |
|---|---|---|---|---|---|
| 1 | crash-and-restart recovery | **PROVEN** | `server serve (host A) & kill -9 mid-export & restart; report project export --publish` | 10-export-amarongi_rwaniro-midflight.json, 11-store-after-kill.json, 12-store-after-restart.json | artifacts registered before the restart=1 (only what the pump had managed to register by the moment it was kill -9'd mid-export, i.e. registration lags sealing): [('network_reporter', 'export-agatare_1', 'held', 'publish-76ab72a6-6b1d-4095-a54e-139232b5152c')]. After restart=4, no duplicate replay_key (after_any_dup=False), nothing present-before now missing (none lost): [['network_reporter', 'export-agatare_1', 'held', 'publish-76ab72a6-6b1d-4095-a54e-139232b5152c'], ['network_reporter', 'export-akaderege_kigoma', 'held', 'publish-e784b29b-b14e-4343-b2d1-7aa9414b2ed4'], ['network_reporter', 'export-akamana', 'held', 'publish-c51893de-ab94-48d2-a9f5-421228f47aa5'], ['network_reporter', 'export-amarongi_rwaniro', 'held', 'publish-876be738-6772-4c0d-a714-3dc1e6402ab1']]. The batch that was mid-flight at the moment of the kill (amarongi_rwaniro, sealed to disk but not yet in the artifacts table when killed) reappeared exactly once after restart, not zero times (lost) and not more than once (duplicated). |
| 2 | idempotent retries | **PROVEN** | `server activity (poll twice, ~30s apart) against the same held artifacts` | 13-store-idempotency-poll.json | 10 receipts recorded across repeated pump polls, all outcome(s)=['failed']; no receipt ever carried a head_revision; artifact row count stayed at 4 (retrying never created a new artifact or duplicated one) -- the retry loop is idempotent at the local-store level, though no retry ever reached a successful commit to compare against (see finding on report.project.export publication being refused by the canary gateway). |
| 3 | concurrent workers (one lease) | **PROVEN** | `server serve --state-dir <hostA> (second instance, different --listen, same store)` | 07-second-writer-refusal.json | a second `ds server serve` pointed at host A's own --state-dir was refused outright: class=conflict code=server_refused message="the existing server connection is on another lane or address; use a separate state directory" -- two pumps can never both attach to one store, so only one can ever hold a project's lease. |
| 4 | interleaved projects | **NOT EXERCISABLE** | `report project export --transformer <tx> --publish (project arjgpydw_survey_test)` | 14-auth-project-use-2.json, 14b-design-status-project2.json, 15-export-interleaved-*.json | every processed transformer tried in the second writable project (arjgpydw_survey_test) failed report export with the same reporter-side defect: class=failed code=print_context_invalid, message='member "tables/alignments.arrow" does not match the canonical schema fingerprint' -- its promoted MV model cannot be reported by this engine build, so no artifact from a second project ever reached the publish queue to test interleaving against. |
| 5 | account/deployment isolation | **NOT EXERCISABLE** | `capabilities --search deployment|fence|"account isolation"; sqlite schema inspection` | 17-search-deployment.json, 17-search-fence.json, 18-store-schema.json | no `ds` command exposes the store's account/deployment fence read-only (searched "deployment", "fence", "account isolation"; closest hits were ['survey.photo.publish', 'auth.logout', 'auth.project.status', 'auth.project.use'], none of which read a store fence). Only one signed-in account was available, so cross-account/cross-deployment publish could not be attempted. Structural evidence only (not a `ds` command): the sqlite store itself keys every row of ['artifacts', 'heads', 'head_reads', 'grants', 'receipts', 'leases'] on (account, deployment, install_id, scope) -- inspected directly per the brief's step 12, not proof of behaviour under a second account. |
| 6 | cancellation/logout consistency | **NOT DISCOVERABLE** | `capabilities --search cancel; server cancel --help; design sync cancel --help` | 19-search-cancel.json, 19-help-server-cancel.txt, 19-help-design-sync-cancel.txt | capabilities --search "cancel" returns ['design.project.cancel', 'design.sync.cancel', 'design.sync.resume', 'design.sync.status', 'design.transformer.retire', 'map.design.batch.save', 'report.layout.session', 'server.cancel', 'solar.application', 'solar.portfolio.batch.cancel']. `server cancel` (server.cancel) only cancels `server submit` compute jobs (table compute_jobs), a different queue from the report-publish artifacts queue this proof exercises. The only cancel for that queue, `design sync cancel` (design.sync.cancel), requires authority=project AND a paired DS GridDesign desktop bridge -- refused here with desktop_operation_unsupported / desktop_not_paired, i.e. it is not a headless surface. No headless command was found to cancel a held-but-not-yet-published report artifact. Logout was not attempted (the brief forbids logging the owner out), so that half of invariant 6 is untested by design, not by gap. |
| 7 | exact acknowledgements | **PROVEN** | `server activity (repeated); direct sqlite read of the receipts table` | 13-store-idempotency-poll.json, 20-durability.json | across 10 receipts spanning a crash, a restart and repeated pump polls, every single one carries outcome="failed" (open_grant refused: 'No published release registers this engine build'; upload refused: 'no work grant covers this publication') and none ever carries a head_revision or reads "published" -- the local artifact state stayed "held" the entire time. The system never marked anything published on the strength of the HTTP round trip alone; it only ever reflects the gateway's actual (negative) commit answer. A true positive (published after a real commit receipt) could not be produced because this canary build is not a registered release at the gateway (see the open_grant failures above) -- an environment/build gate, not a defect this proof can fix. |

## What a stranger could not find

- `ds diagnostics identity` (as the brief's own example phrasing suggested): refused with `unknown_domain` -- "diagnostics" is not a domain; the equivalent live data came from `ds doctor` instead.
- `ds capabilities --search "pump"`: 0 results -- the word "pump" is not discoverable; the mechanism a stranger would find by reading `report project export --help` and `server serve --help` is that `server serve` IS the pump (its own help text says "This process runs in the foreground" and `report project export --publish`'s note says "the matching native Server sync pump publishes when it next runs").
- No command cancels a held-but-unpublished report-artifact headlessly (see invariant 6 above); the only cancel for that queue is desktop-paired.
- No command reads the sync store's account/deployment fence read-only (see invariant 5 above); `auth.project.status` shows only the caller's own selected project, not the store's fence.
- `ds design sync status` / `ds desktop sync status`: both refused with `desktop_operation_unsupported` ("the paired local Desktop build has no provisioned native lane") -- these are desktop-bridge commands, not a headless path, even though their summaries ("Inspect retained reconciliation operations offline") read as if they might be.

## Durability of the store itself

- File(s): `/home/magese/data-solutions/_worktrees/proofs-20260922/sync-up/runs/canary-873f299/hostA/store.sqlite-shm
/home/magese/data-solutions/_worktrees/proofs-20260922/sync-up/runs/canary-873f299/hostA/store.sqlite
/home/magese/data-solutions/_worktrees/proofs-20260922/sync-up/runs/canary-873f299/hostA/store.sqlite-wal` -- created at `server serve` startup, before any work was queued.
- `pragma integrity_check` -> [['ok']]; `pragma journal_mode` -> [['wal']].
- Tables: ['meta', 'artifacts', 'heads', 'head_reads', 'grants', 'receipts', 'sqlite_sequence', 'leases', 'compute_jobs'].
- Every row in `artifacts`, `heads`, `head_reads`, `grants`, `leases` is keyed by (account, deployment, install_id, scope, ...): ['artifacts', 'heads', 'head_reads', 'grants', 'receipts', 'leases'].
- The **acknowledgement side** (receipts, leases, artifact state) lives entirely in this SQLite file (WAL mode). The **downloaded/staged side** (the actual report bytes: shp/kmz/xlsx/pdf per transformer, plus `publication-batches/<batch_id>/publication-receipt.json`) lives in a **separate**, plain-filesystem, content-hash-named directory tree next to it (`report-artifacts/` under the same --state-dir), not inside the sqlite store:
  - `/home/magese/data-solutions/_worktrees/proofs-20260922/sync-up/runs/canary-873f299/hostA/report-artifacts` ... (19 entries total; see 21-report-artifacts-listing.txt)
  This split (SQLite for sync/lease/receipt bookkeeping, filesystem content-store for the bytes it bookkeeps) is itself the answer to "does what-to-download-and-cache need a SQLite transaction": the **acknowledgement** that something is queued/held/published is already transactional SQLite; the **bytes** are still plain files staged by content hash, with no separate journal of their own beyond the per-artifact `artifact-receipt.json` sitting next to each blob.

## Host B (fresh state-dir, same account) -- pull vs recompute

- `server activity` on a brand-new `--state-dir` for the same project returned: `projects=[]`.
- Because no artifact from host A ever reached a **published** state at the gateway (see invariant 7), there was nothing durably published for host B to pull; this step could not distinguish "pulled" from "recomputed" for the same reason invariants 2/4/7's positive cases are blocked. Recorded as **NOT PROVEN** (blocked, not refuted) pending a build the gateway will register.

## Exact list of what was written to the cloud / left in state

- Locally sealed + queued for publish (never confirmed published by the gateway): project `agct38sq_sample`, transformers `agatare_1`, `akaderege_kigoma`, `akamana`, `amarongi_rwaniro` (each: shp+kmz+xlsx+pdf, held in host A's local report-artifacts store; visible in the owner's canary UI, if at all, only as locally-sealed/queued, never as a new published revision).
- Selected-project side effects: `ds auth project use` was called against the account's own canary credential to switch between `agct38sq_sample` and `arjgpydw_survey_test`, and restored to the originally-selected project at the end of the run (see 25-auth-project-restore.json).
- No compounded/archived report was published; no transformer was retired, restored, or edited; no feedback closed; no merges or pushes.

## Cleanup

- Processes still matching `state-dir runs/canary-873f299` after cleanup: `(none)`.
